# Часть 1
1) Создать статический PersistentVolume (https://minikube.sigs.k8s.io/docs/handbook/persistent_volumes/)
2) Создать PersistentVolumeClaim, который запросит
- объем `1Gi`
- политику доступа `ReadWriteOnce`
3) Создать манифест пода nginx, который монтирует логи в PVC (`/data`); подергать nginx в контейнере curl, проверить что логи пишутся; пересоздать pod и проверить что старые логи на месте

# Часть 2
1) Создать сервис `mongo-svc` с `clusterIP: None` и меткой `app: mongo`
2) Создать Stateful Set для mongodb с PVC размером `1Gi` и политикой доступа `ReadWriteOnce`
3) Запустить все это дело и проверить (как - подумать самостоятельно)
