1) Создать через манифест pod с контейнером из docker image `anestesia01/vovcheck-k8s:echo-server`; \
Зайти внутрь и проверить, что приложение отвечает на `curl localhost`
2) Создать через манифест pod с контейнером из docker image `anestesia01/vovcheck-k8s:log-alive`; \
Посмотреть логи контейнера внутри пода.  Открыть еще одну вкладку терминала, подцепиться к логам пода. В другой вкладке зайти в контейнер и убить главный процесс.
Проверить, выплюнул ли под логи об "убийстве" и был ли перезапущен
3) Создать два пода с `anestesia01/vovcheck-k8s:echo-server` и создать для них Service. Проверить доступ к подам, через сервис.
4) Написать ReplicaSet для `anestesia01/vovcheck-k8s:echo-server` с 3 репликами. Отстрелить один pod вручную и посмотреть, запустит ли RS новый. Отмасштабировать replicaset до 1 реплики.
Отмасштабировать replicaset до 4 реплик. Все изменения смотреть через `kubectl get pods -w`. Добавить сервис и проверить, что при изменении количества реплик доступ к приложению не прерывается
5) Написать манифест Deployment для docker image `anestesia01/vovcheck-k8s:echo-server-vers`. Приложению нужна переменная `APP_VERSION`; значение любое (`1.0`, `1.2.0` и тп). Прикрутить к этому service \
Проверить, что приложение отдает версию из переменной
```bash
curl -X POST -H "X-Custom: test" -d '{"key":"value"}' http://localhost:8080/api/test
```
Запустить `kubectl logs -f deployment/echo-server`. Изменить в манифесте `APP_VERSION` и применить манифест (в отлельной вкладке)
